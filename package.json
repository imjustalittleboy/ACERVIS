{
  "name": "acervis-protocol",
  "version": "3.1.0",
  "type": "module",
  "description": "Pan-Nigerian Blockchain Academic Credential & Transcript Verification Protocol",
  "main": "index.html",
  "scripts": {
    "start": "serve .",
    "deploy:contract": "node scripts/deploy.cjs",
    "deploy:contract:setup": "node scripts/deploy.cjs --setup",
    "compile": "npx hardhat compile",
    "deploy:hardhat": "npx hardhat run scripts/deploy.cjs --network amoy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.3",
    "ethers": "^6.12.1",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^3.9.0",
    "serve": "^14.2.3"
  }
}
